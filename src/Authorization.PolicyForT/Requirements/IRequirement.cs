namespace Authorization.PolicyForT.Requirements;

/// <summary>
/// Interface for tagging a class as requirement 
/// so it can be picked up by the authorizer
/// </summary>
public interface IRequirement { }